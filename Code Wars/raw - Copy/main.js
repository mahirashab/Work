function add(first, secoend) {
    if()
    
    for(let i = first.length - 1; i >= 0; i++) {
        for (let j = secoend.length - 1; j >= 0; j++) {
            let num1 = Number(first[i])
            let num2 = Number(secoend[i])

        }
    }
}